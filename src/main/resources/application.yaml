server:
  port: 8283

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order-service
    username: postgres
    password: myPassword
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
